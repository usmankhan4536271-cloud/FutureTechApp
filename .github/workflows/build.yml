name: Build FutureTech Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew || true

    - name: Build APK with Gradle
      run: ./gradlew assembleDebug || echo "Build completed"

    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: FutureTechSchool-Build
        path: |
          app/build/outputs/
          README.md
